# .env.example

# --- 域名設定 (請務必修改為您自己的域名) ---
# Docker Registry 的公開域名
REGISTRY_DOMAIN=registry.your-domain.com
# Registry UI 的公開域名
REGISTRY_UI_DOMAIN=ui.your-domain.com
# Traefik Dashboard 的域名 (可選，建議設定為內部或受保護的域名)
TRAEFIK_DASHBOARD_DOMAIN=traefik.your-domain.com

# --- Registry 認證設定 ---
# 登入 Registry 使用的帳號
REGISTRY_USER=admin
# 登入 Registry 使用的密碼 (建議使用高強度密碼)
REGISTRY_PASSWORD=YourStrongPasswordHere

# --- UI 設定 ---
REGISTRY_UI_TITLE=My Private Registry

# --- 服務端口設定 ---
# Traefik 對外監聽的 HTTP 端口 (Cloudflare Tunnel 將會連到這個端口)
TRAEFIK_WEB_PORT=8000
# Traefik Dashboard 的對外端口 (可透過 http://<nas-ip>:<port> 存取)
TRAEFIK_API_PORT=8081
# Registry UI 服務在容器內部的端口 (一般無需修改)
REGISTRY_UI_INTERNAL_PORT=80